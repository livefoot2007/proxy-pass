{
  "name": "simple-http-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Simple configurable HTTP reverse proxy",
  "scripts": { "start": "node server.js" },
  "engines": { "node": ">=18" }
}
